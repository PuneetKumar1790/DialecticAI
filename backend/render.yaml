# Deploy configuration for Render
# Documentation: https://render.com/docs

services:
  - type: web
    name: dialecticai-backend
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: GROQ_API_KEY
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: FRONTEND_URLS
        value: https://dialectic-ai.vercel.app,http://localhost:5173,http://localhost:8000
